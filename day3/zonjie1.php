<?php 
// define('SYSTEM_NAME', 'xxx',true);//xxx
//define('SYSTEM_ENABLE', true);//1
//echo SYSTEM_ENABLE;

// 载入其他文件
//require
//require_once
//include
//include_once
//include 一般用于载入公共文件，这个文件的存在与否不能影响程序后面的运行
//require 用于载入不可缺失的文件
//至于是否采用一次载入（once）这种方式取决于被载入的文件

//$arr=' ewdrfgbcsxa ';
$str='呵呵哈哈哈';
 	// echo substr($arr, 1,6);//截取字符串
	// echo strlen($arr);//字符串的长度
	echo mb_strlen($str);
	// echo strtolower($arr);//字符串小写
	// echo strtoupper($arr);//字符串大写
	// echo trim($arr);//去除字符串两边的空格
	// echo str_replace('fg', 'ab', $arr);str_replace('要替换的字符','替换成的字符'，'传入的字符串')
	//echo str_repeat($arr, 2);//str_repeat(要重复的字符串, 重复的次数)
	//$arr=Array(1,7,8,9,12);
	//var_dump(array_unique($arr)); //数组去重
	// array_push($arr,12);//在数组后面添加一个数
	// array_pop($arr);//删除数组中最后一个数
	// echo count($aar);//数组长度
	// print_r(in_array(12, $arr)) ;//检查数组中是否存在某个值
	// echo time();//时间戳以秒为单位
	// 格式化日期： date()
	// 获取有格式的当前时间
	// 格式化一个指定的时间戳
	// 可以通过 strtotime() 将有格式的时间字符串转换为时间戳
	// phpinfo();//查看php配置信息
	// echo date('Y-m-d h:i:s');


